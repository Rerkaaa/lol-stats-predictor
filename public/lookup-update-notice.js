(() => { const hero=document.querySelector('#lookupView .matches-hero p:not(.eyebrow)'); if(hero) hero.textContent='Look up a player’s current solo rank and recent League of Legends games by Riot ID.'; const form=document.querySelector('#lookupForm'); if(form&&!document.querySelector('.lookup-update-notice')){const note=document.createElement('aside');note.className='lookup-update-notice';note.innerHTML='<b>Latest update</b><span>Ranked role performance was added. Match Analysis now includes timelines, builds, runes, objectives, and player impact.</span>';form.insertAdjacentElement('beforebegin',note)}})();
