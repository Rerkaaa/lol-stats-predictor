(() => { let match = null; const base = window.fetch.bind(window); window.fetch = async (...args) => { const response = await base(...args); if (String(args[0] instanceof Request ? args[0].url : args[0]).includes('/api/summoner/match?')) { try { match = await response.clone().json(); } catch { match = null; } } return response; };
  const graph = (id, metric) => { const data = (match.timelinePlayers || []).map((frame) => ({ minute: frame.minute, value: frame.players.find((player) => player.participantId === id)?.[metric] || 0 })), max = Math.max(1, ...data.map((row) => row.value)); return `<svg viewBox="0 0 640 180"><polyline points="${data.map((row, index) => `${24 + index * 592 / Math.max(1, data.length - 1)},${156 - row.value / max * 132}`).join(' ')}"/><text x="24" y="173">0m</text><text x="590" y="173">${data.at(-1)?.minute ?? 0}m</text></svg>`; };
  const add = () => document.querySelectorAll('.full-match-panel:not([data-player-progress])').forEach((panel) => { if (!match?.timelinePlayers) return; panel.dataset.playerProgress = 'true'; const section = document.createElement('section'); section.className = 'player-progression'; let player = match.players[0], metric = 'gold'; const draw = () => { section.querySelector('.progress-chart').innerHTML = graph(player.participantId, metric); section.querySelector('.progress-title').textContent = `${player.champion} — ${metric === 'xp' ? 'XP' : metric === 'cs' ? 'CS' : 'Gold'} over time`; }; section.innerHTML = `<h3 class="progress-title"></h3><div class="progress-players">${match.players.map((item) => `<button data-player="${item.participantId}">${item.champion}</button>`).join('')}</div><div class="timeline-metric-tabs"><button class="active" data-progress="gold">Gold</button><button data-progress="xp">XP</button><button data-progress="cs">CS</button></div><div class="progress-chart"></div>`; section.querySelectorAll('[data-player]').forEach((button) => button.addEventListener('click', () => { player = match.players.find((item) => item.participantId === Number(button.dataset.player)); draw(); })); section.querySelectorAll('[data-progress]').forEach((button) => button.addEventListener('click', () => { metric = button.dataset.progress; section.querySelectorAll('[data-progress]').forEach((tab) => tab.classList.toggle('active', tab === button)); draw(); })); draw(); panel.append(section); }); new MutationObserver(() => requestAnimationFrame(add)).observe(document.querySelector('#lookupResult'), { childList: true, subtree: true }); })();
