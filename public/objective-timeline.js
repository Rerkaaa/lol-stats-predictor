(() => {let match=null;const base=window.fetch.bind(window);window.fetch=async(...args)=>{const response=await base(...args);if(String(args[0] instanceof Request?args[0].url:args[0]).includes('/api/summoner/match?')){try{match=await response.clone().json()}catch{match=null}}return response};const add=()=>document.querySelectorAll('.full-match-panel:not([data-objectives])').forEach(panel=>{if(!match?.objectiveEvents)return;panel.dataset.objectives='true';const section=document.createElement('section');section.className='objective-timeline';section.innerHTML=`<h3>Objectives timeline</h3><div>${match.objectiveEvents.map(event=>`<article class="${event.teamId===100?'blue':'red'}"><b>${event.minute}m</b><span>${event.label}</span></article>`).join('')||'<p>No objective events available.</p>'}</div>`;panel.append(section)});new MutationObserver(()=>requestAnimationFrame(add)).observe(document.querySelector('#lookupResult'),{childList:true,subtree:true})})();
